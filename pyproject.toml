[tool.poetry]
name = "sl_parsing_slp"
version = "0.1.0"
description = ""
authors = ["Hiroyuki Otomo"]

[tool.poetry.dependencies]
python = "^3.9"
numpy = "^1.22.2"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
